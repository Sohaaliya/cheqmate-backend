# Disable generation of source maps to avoid Webpack warnings
GENERATE_SOURCEMAP=false

# Optional: Set the React app port (default is 3000)
PORT=3000

# Optional: API URL for backend requests
REACT_APP_API_URL=http://localhost:8080
